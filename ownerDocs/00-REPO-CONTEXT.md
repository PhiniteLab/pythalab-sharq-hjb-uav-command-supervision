# Repository context

**One page that orients an agent before it reads anything else in this repository.**
Read after `ownerDocs-Rules.md`, before touching code or manuscripts.

> Owner-owned and repository-specific. The template ships this file EMPTY; the scaffold
> creates it once and never overwrites it, and `--sync` never touches it. Fill it in your
> own words — the headings below are a prompt, not a schema.

---

## What this repository is

<!-- One paragraph. What problem it addresses, and what it is NOT. -->

## Scientific claim under development

<!-- The central claim in one sentence, stated so it could be wrong.
     "X improves Y under conditions Z" beats "we study X". -->

## Where the truth lives

<!-- Which directories carry the real state, so an agent does not guess:
     - code that runs the experiments      -> path
     - the current method specification    -> myMethods/<highest number>
     - results an outside reader can trust -> path
     - the corpus                          -> myLiterature/MANIFEST.yaml -->

## Toolchain and how to run it

<!-- The exact commands. Environment, entry point, the one smoke command that proves the
     repository still works, and how long it takes. -->

## Constraints and non-goals

<!-- Hardware ceilings, data that must not leave the machine, venue or deadline
     constraints, and what has been deliberately ruled out (with the reason). -->

## Current blockers

<!-- What is actually stopping progress right now. Empty is a valid answer, and an
     honest one. -->
