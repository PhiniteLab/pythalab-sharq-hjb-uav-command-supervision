# myLiterature

- `pdfs/` — corpus PDFs (gitignored via `ownerDocs/myLiterature/pdfs/`); Claude maintains `manifest.json`.
- `review/` — consolidated literature reviews; Claude writes, owner evaluates.
